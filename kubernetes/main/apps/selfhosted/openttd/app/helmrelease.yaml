---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openttd
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
    - name: volsync
      namespace: volsync-system
  values:
    controllers:
      openttd:
        annotations:
          reloader.stakater.com/auto: 'true'
        containers:
          app:
            image:
              repository: ghcr.io/sillock1/openttd
              tag: 14.1@sha256:43e80c5b0065f18f4dd10b9b473cfc6aa4ab1040b02bbc5a10a8f61adabcbe21
            env:
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
                memory: 250Mi
              limits:
                memory: 2Gi
            probes:
              liveness: &probes
                enabled: false
              readiness: *probes
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 2048
        runAsGroup: 2048
        fsGroup: 2048
        fsGroupChangePolicy: OnRootMismatch
        supplementalGroups: [2048]
        seccompProfile: { type: RuntimeDefault }
    service:
      app:
        controller: openttd
        ports:
          tcp:
            port: 3979
            protocol: TCP
            targetPort: 3979
          udp:
            port: 3979
            protocol: UDP
            targetPort: 3979
    ingress:
      app:
        className: external
        hosts:
          - host: &host "{{ .Release.Name }}.sillock.io"
            paths:
              - path: /
                service:
                  identifier: app
                  port: 3979
    persistence:
      config-file:
        type: configMap
        name: openttd-configmap
        globalMounts:
          - path: /home/openttd/openttd.cfg
            subPath: openttd.cfg
            readOnly: true
      config:
        existingClaim: openttd
        advancedMounts:
          openttd:
            app:
              - path: /home/openttd
