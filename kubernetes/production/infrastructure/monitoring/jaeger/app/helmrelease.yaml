---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: jaeger
spec:
  interval: 30m
  timeout: 15m
  chart:
    spec:
      chart: jaeger
      version: 0.71.14
      sourceRef:
        kind: HelmRepository
        name: jaeger
        namespace: flux-system
      interval: 30m
  values:
    provisionDataStore:
      cassandra: false
    allInOne:
      enabled: true
    storage:
      type: none
    agent:
      enabled: false
    collector:
      enabled: false
    query:
      enabled: false
