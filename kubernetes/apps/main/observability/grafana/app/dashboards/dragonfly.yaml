---
# yaml-language-server: $schema=https://kubernetes.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: dragonfly-operator
spec:
  instanceSelector:
    matchLabels:
      dashboards: "cluster"
  datasources:
    - inputName: "DS_GRAPHITE"
      datasourceName: "prometheus"
  # renovate: depName="Dragonfly Dashboard"
  url: https://raw.githubusercontent.com/dragonflydb/dragonfly-operator/refs/tags/v1.2.1/monitoring/grafana-dashboard.json
