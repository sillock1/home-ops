apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dotbot
spec:
  endpoints:
    - interval: 10s
      path: /metrics
      port: 80
      scheme: http
  namespaceSelector:
    matchNames:
      - opentoucan
